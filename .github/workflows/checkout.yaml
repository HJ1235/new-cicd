name: Checkout Workflow

on: push

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -al
      - uses: actions/checkout@v3
        with:
          path: source
      - run: ls -al source
      - run: cat source/.github/workflows/checkout.yaml
